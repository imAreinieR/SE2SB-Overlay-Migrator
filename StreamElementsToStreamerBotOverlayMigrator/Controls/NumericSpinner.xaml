<UserControl x:Class="StreamElementsToStreamerBotOverlayMigrator.Controls.NumericSpinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="30">

    <UserControl.Resources>

        <Style x:Key="SpinnerInput" TargetType="TextBox">
            <Setter Property="Background" Value="#0f111a"/>
            <Setter Property="Foreground" Value="#d0d6f0"/>
            <Setter Property="CaretBrush" Value="#4f7eff"/>
            <Setter Property="BorderBrush" Value="#252c40"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <!-- Square right corners — spin border provides that edge -->
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4,0,0,4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="8,0"
                                          VerticalAlignment="Center"
                                          VerticalScrollBarVisibility="Hidden"
                                          HorizontalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#4f7eff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SpinBtn" TargetType="RepeatButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#5a6280"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#252c40"/>
                                <Setter Property="Foreground" Value="#d0d6f0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#2e3650"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Height="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Value input -->
        <TextBox x:Name="ValueBox"
                 Grid.Column="0"
                 Style="{StaticResource SpinnerInput}"
                 TextChanged="ValueBox_TextChanged"/>

        <!-- Spin buttons — right cap of the composite control -->
        <Border Grid.Column="1"
                Width="22"
                Background="#1a1e2b"
                BorderBrush="#252c40"
                BorderThickness="1,1,1,1"
                CornerRadius="0,4,4,0"
                ClipToBounds="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="1"/>   <!-- Divider -->
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <RepeatButton x:Name="UpBtn"
                              Grid.Row="0"
                              Style="{StaticResource SpinBtn}"
                              Click="UpBtn_Click">
                    <TextBlock Text="▲" FontSize="7"/>
                </RepeatButton>

                <!-- Divider -->
                <Border Grid.Row="1" Background="#252c40"/>

                <RepeatButton x:Name="DownBtn"
                              Grid.Row="2"
                              Style="{StaticResource SpinBtn}"
                              Click="DownBtn_Click">
                    <TextBlock Text="▼" FontSize="7"/>
                </RepeatButton>
            </Grid>
        </Border>

    </Grid>
</UserControl>
